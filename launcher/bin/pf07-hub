#!/usr/bin/env sh
set -eu
script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
package_dir=$(CDPATH= cd -- "$script_dir/../.." && pwd)
PYTHONPATH="$package_dir/launcher${PYTHONPATH:+:$PYTHONPATH}" exec python3 -m pf07_launcher.hub "$@"
