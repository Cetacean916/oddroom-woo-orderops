# OFFSET OrderOps · macOS universal app entry

1. Extract the complete ZIP into one folder and double-click `PF07 Launcher.app`. You may place the complete package folder under Applications; do not move the app alone because it needs the adjacent shared launcher and payload.
2. If macOS blocks the downloaded app, Control-click it in Finder, choose `Open`, then confirm `Open`. This zero-cost portfolio artifact is unsigned and not notarized; paid signing is not applied.
3. If the launcher reports a missing Python or container runtime prerequisite, use its numbered official link. The maintained 0-KRW path is Rancher Desktop with the Moby engine.
4. After installation, logout, or restart, open the app again. It reads package-local progress, rechecks, and resumes.
5. Use the hub for Korean/English, mode, start, store, admin, stop, recovery, backup/restore, controlled update, optional HTTPS tunnel through an external provider CLI, and evidence export.

Command fallbacks are `pf07.command` for the hub and CLI commands such as `./pf07 status`. Apple Silicon and Intel use the same portable POSIX app adapter and the same Python launcher core. The artifact is validated for app structure, permissions, architecture declaration, shared core, and archive boundaries; it does not claim actual Mac container-stack or Safari execution.
