#!/bin/sh
set -eu
script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$script_dir/launcher${PYTHONPATH:+:$PYTHONPATH}" exec python3 -B -m pf07_launcher.cli "$@"
