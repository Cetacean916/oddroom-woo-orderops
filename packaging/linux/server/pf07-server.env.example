# Copy this file to server/pf07-server.env before first start.
# The server package always defaults the WordPress listener to 127.0.0.1.
# This port is stored in .pf07/runtime.env on first start; changing this file
# afterward does not migrate an existing runtime.
PF07_WORDPRESS_PORT=19081
