# PF07 server binds WordPress to loopback by default. Put an authenticated reverse proxy in front.
PF07_BIND_ADDRESS=127.0.0.1
PF07_WORDPRESS_PORT=19081
# Optional public HTTPS base used only after the reviewed tunnel/reverse-proxy path is enabled.
PF07_PUBLIC_HTTPS_BASE=
